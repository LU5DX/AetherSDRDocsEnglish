# Add a memory from the active slice using the panel button

Save the current active slice as a memory entry in the radio’s memory list so you can recall it later.

## Before you start

- A radio must be connected.
- The Memory Browser panel must be visible in the MainWindow side panel.

## Steps

1. Tune the active slice to the frequency and mode you want to save.
2. In the Memory Browser panel, click **Add Memory**.

The slice’s frequency and name are saved as a new memory entry, which appears immediately in the memory table.

## What each control does

| Control | Behavior |
|---------|----------|
| **Memory table** | Shows each memory sorted by frequency; single-click or Enter to activate the tuned slice to that memory. Columns: Frequency, Name. The closest memory to the current tuning is highlighted with a distinct background. Double-click also tunes. |
| **Add Memory** | Saves the current slice as a new memory entry. Positioned at the top of the panel, immediately above the memory table rows. Tooltip: “Save the current slice on this panadapter as a memory.” |
| **No memories are available yet.** | Shown when the radio has no memories loaded. |

## Tips

- The memory is stored on the radio itself, not locally — the name and frequency come from the active slice’s current settings.
- The **Add Memory** button is always visible at the top of the panel, above the scrolling memory table.

## Related

- [Memory Browser overview](overview.md)
- [Browse the radio's stored memories](browse-the-radio-s-stored-memories.md)
- [Jump to the memory closest to the current frequency](jump-to-the-memory-closest-to-the-current-frequency.md)
- [Activate a memory with a single click](activate-a-memory-with-a-single-click.md)